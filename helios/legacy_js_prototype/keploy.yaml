# Keploy configuration for local record & replay
version: "v1"
app:
  port: 3000
  # framework: nodejs
testcase:
  path: keploy/testcases
  # mode: record | test (set via CLI or env)
mock:
  path: keploy/mocks

