# Minimal Trunk config for serving static HTML
# WELCOME is now pure HTML with no Rust/WASM compilation

[build]
target = "index.html"
dist = "dist"
public_url = "/"

[serve]
addresses = ["127.0.0.1"]
port = 8080

[watch]
watch = ["index.html", "about.html", "assets"]
